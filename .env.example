# Supabase credentials (required)
# Get these from your Supabase project: Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# Splitwise API credentials (optional — needed only for Splitwise features)
# Register an app at https://secure.splitwise.com/apps
SPLITWISE_CONSUMER_KEY=your-splitwise-consumer-key
SPLITWISE_CONSUMER_SECRET=your-splitwise-consumer-secret
# Access token for Splitwise API (optional — needed for authenticated requests)
SPLITWISE_ACCESS_TOKEN=your-splitwise-access-token

# Server port (optional, defaults to 3001)
PORT=3001
